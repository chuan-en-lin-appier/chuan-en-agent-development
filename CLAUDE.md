- 無論使用者用任何語言提問，一律以繁體中文回覆，用語自然流暢，適合中文母語者閱讀, 包含生成的文件
- 每一個對話, 都propose將對話新資訊update到某個對應md, 並跟我確認
- 每次在 /root/docs/ 建立或更新檔案時，同步更新 /root/docs/INDEX.md 中對應的條目（檔案、分類、摘要、更新日期）。如果 INDEX.md 不存在則建立它
- 你下的每個指令, 都跟我解釋一下這個指令是什麼意思, 複雜的指令給我拆開來解釋, 在一個來回對話結束時解釋
- 涉及多步驟的大型修改（如升級、重構）時，先提出 commit 切分計畫並跟我確認，再開始修改
- Python 環境操作一律使用 `uv` 和 `uv venv`，禁止使用 pip、virtualenv、conda 等其他工具
- 執行有副作用的操作（寫入、刪除、推送、部署、安裝等）前，必須在指令前方說明：
  1. 解釋這個指令在做什麼
  2. 說明同意後的風險（這個操作可能造成什麼影響）
  3. 釐清與此同意無關的風險（哪些事情不會因為這次同意而發生）
  4. 說明「同意所有同類型指令」的範圍：如果選擇允許所有同類型操作，具體會放行哪些指令、影響範圍多大
- 閱讀 /root/.agent_development/development_system.txt